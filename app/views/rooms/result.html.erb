<%= javascript_include_tag "rooms.js" %>
<div class="center70">
	<h1><%= @room.name %></h1>
	<div>参加人数　<%= @number_of_joined %>人</div>
	<h2>ビンゴ結果発表</h2>
	<div class="white-box">
		<h3>ビンゴしたユーザー一覧 <button type="button" class="btn btn-primary" onclick="view_mail_address()">メールアドレス表示</button></h3>
		<% if !@bingo_list.blank? %>
		<% @bingo_list.each_with_index do |user, i| %>
		<div>
			<div>
				<div><%= i+1 %>位</div>
				<%= link_to user.name, user_path(user.user_id), target: "_new" %> さん
				<span class="view-mail-addess" style="display: none;">
					<%= user.email %>
				</span>
				<span><%= user.note %></span>
			</div>
			<div><%= user.times %>回目 <%= user.seconds %>ms</div>
		</div>
		<% end %>
		<% end %>
	</div>
	<p>このページは部屋作成後、１週間で削除されます。</p>
	<%= link_to "マイページに戻る", pages_user_index_path %>
	<br>

</div>